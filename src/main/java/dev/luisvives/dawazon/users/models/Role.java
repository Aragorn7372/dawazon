package dev.luisvives.dawazon.users.models;

/**
 * Enum que define los roles de usuario en el sistema.
 * <p>
 * Roles disponibles: USER (usuario regular), ADMIN (administrador), MANAGER
 * (gestor).
 * </p>
 */
public enum Role {
    USER, ADMIN, MANAGER;
}
